// This class was generated by malg (using Twig)!
//                <#if willBeOverwritten=true>DO NOT modify it, it will be overwritten!<#else>
//                You can modify it, it won't be overwritten!</#if>
package {{package}};
{% include "imports.twig" with { imports: proxy.imports } %}
{% include "proxy_annotations.twig" %}
public class {{proxy.name}}
{
	protected static Logger logger = Logger.getLogger({{proxy.name}}.class.getCanonicalName());

	{% for item in proxy.storedProcedures %}
{% include "proxy_method.twig" with { m: item } %}
    {% endfor %}
}